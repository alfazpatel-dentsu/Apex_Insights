# Local/deploy params for project vdc200007-ppclientcentre-prod
# Copy values into: functions/.env.vdc200007-ppclientcentre-prod
# (that file is gitignored — do not commit the real key)

SHEETS_SPREADSHEET_ID=1NnLAuCjA4ZeaH116jzbVVajkYX3lytxbZVxOGocLWSs
SHEETS_TAB_NAME=ActionItems

# Base64 of the service-account JSON (single line). Generate with:
#   base64 -w 0 /path/to/your-sa-key.json
# Then paste the output after the = below (no quotes).
SHEETS_SERVICE_ACCOUNT_JSON_B64=
